packages:
  - name: shaastra
    actions:
       - name: backend
         runtime: 'nodejs:default'
         limits:
           timeout: 400000
           memory: 256
environment:
  username: ${username}
  password: ${password}
  host: ${host}
  port: ${port}
  database: ${database}
  sslmode: ${sslmode}