packages:
  - name: shaastra
    actions:
      - name: backend
        limits:
          timeout: 5000
          memory: 256
environment:
  username: ${username}
  password: ${password}
  host: ${host}
  port: ${port}
  database: ${database}
  sslmode: ${sslmode}